import { EnumKeyFormatPipe } from './enum-key-format.pipe';

describe('EnumKeyFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new EnumKeyFormatPipe();
    expect(pipe).toBeTruthy();
  });
});

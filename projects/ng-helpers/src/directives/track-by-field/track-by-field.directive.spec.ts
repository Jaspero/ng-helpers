import {TrackByFieldDirective} from './track-by-field.directive';

describe('TrackByFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new TrackByFieldDirective();
    expect(directive).toBeTruthy();
  });
});

import { DebounceChangeDirective } from './debounce-change.directive';

describe('DebounceChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new DebounceChangeDirective();
    expect(directive).toBeTruthy();
  });
});
